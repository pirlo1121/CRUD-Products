const { Router } = require( 'express' );
const router = Router();





router.post('/', '');
router.get('/', '');
router.get('/:id', '');
router.delete('/:id', '');
router.patch('/:id', '');


module.exports = router;